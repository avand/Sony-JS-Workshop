"use strict";

var people = ["bill", "bob", "jerry", "barbara", "john", "bill", "sarah", "bob", "bill"];

// Use filter to find all the people whose name starts with a "b"
// Tip: "asdf"[0] => "a"

// Use map to return an array with the length of everyone's name

// Use reduce to count the number of characters in all the people's names
